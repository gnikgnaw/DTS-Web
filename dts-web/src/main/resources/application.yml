spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dts?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password:
  jackson:
    default-property-inclusion: non_null
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8


server:
  port: 8080

